// 23-01-2025

// Arrays.

// A vector (or onedimentional array) is a data-type that storages a secuence of data of the same kind.

//The elements of the secuence are almacenated in near locations along the memory. We're able to acces to 
// elements of the array by calling thems with their index (a position).

// I order to define the vector we need:


// type name[size]; int numeros[10]; char letra[5]; float reales[15]. 

#include<iostream>

using namespace std;

#include<iostream>

#include<conio.h>


int main(){
	// Here I'll define some arrays.
	int numero[]={1,4,6,7,3};
	
	char letra[]={'a','e','i','o','u'};
	
	return 0;
}
